/**
 * Các vai trò minh họa AuthZ — mở rộng được nhưng giữ demo nhỏ gọn.
 * (EN: RBAC roles for lesson clarity — admin/editor/user/viewer.)
 */
export enum Role {
    ADMIN = "admin",
    EDITOR = "editor",
    USER = "user",
    VIEWER = "viewer",
}
